import { ListaAlumnosDirective } from './lista-alumnos.directive';

describe('ListaAlumnosDirective', () => {
  it('should create an instance', () => {
    const directive = new ListaAlumnosDirective();
    expect(directive).toBeTruthy();
  });
});
